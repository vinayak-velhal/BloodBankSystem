@model BloodBankManagementSystem.Models.DoctorInformation

@{
    ViewData["Title"] = "Doctor Details";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">

            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
                    <h4 class="mb-0"><i class="bi bi-info-circle-fill me-2"></i>Doctor Details</h4>
                </div>

                <div class="card-body px-5 py-4">
                    <dl class="row mb-0">
                        <dt class="col-sm-4 fw-semibold text-secondary">Doctor Id</dt>
                        <dd class="col-sm-8">@Model.DoctorId</dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">Doctor Name</dt>
                        <dd class="col-sm-8 text-primary fw-semibold">@Model.DoctorName</dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">Mobile No</dt>
                        <dd class="col-sm-8">@Model.MobileNo</dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">Doctor Address</dt>
                        <dd class="col-sm-8">@Model.DoctorAddress</dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">Qualification</dt>
                        <dd class="col-sm-8">@Model.Qualification</dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">Email Id</dt>
                        <dd class="col-sm-8">@Model.EmailId</dd>
                    </dl>
                </div>

                <div class="card-footer bg-light text-end rounded-bottom-4 py-3 px-4">
                    <a asp-action="Edit" asp-route-id="@Model.DoctorId" class="btn btn-warning text-dark me-2 shadow-sm">
                        <i class="bi bi-pencil-square me-1"></i> Edit
                    </a>
                    <a asp-action="Index" class="btn btn-outline-secondary shadow-sm">
                        <i class="bi bi-arrow-left-circle me-1"></i> Back to List
                    </a>
                </div>
            </div>

        </div>
    </div>
</div>



